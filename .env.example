# ===== App =====
NODE_ENV=development
PORT=3000

# ===== Database (PostgreSQL 14) =====
# Prisma connection string. URL-encode special chars in the password (@ -> %40).
DATABASE_URL="postgresql://postgres:your_password@localhost:5432/HR_App?schema=public"

# ===== JWT =====
JWT_SECRET=change-this-to-a-long-random-secret
JWT_EXPIRES_IN=1d

# ===== Auth cookie (httpOnly) & CORS =====
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
COOKIE_NAME=access_token
COOKIE_SECURE=false
COOKIE_MAX_AGE_MS=86400000

# ===== Localization =====
DEFAULT_LANG=lo

# ===== Firebase push (optional) =====
# Absolute path to the Firebase service-account JSON (Project settings ->
# Service accounts -> Generate new private key). Leave empty for in-app only.
FIREBASE_SERVICE_ACCOUNT=

# ===== Seed (login is by email) =====
SEED_ADMIN_EMAIL=admin@hrapp.la
SEED_ADMIN_USERNAME=admin
SEED_ADMIN_PASSWORD=admin123
